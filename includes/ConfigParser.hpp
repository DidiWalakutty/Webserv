#pragma once

#include "Config.hpp"
#include <string>
#include <vector>

// The ConfigParser class will be responsible for parsing configuration files:
// - Open and read the configuration file
// - Parse the content line by line
// - Store the parsed data into ServerConfig and LocationConfig structures
// - Handle syntax errors and invalid configurations

// Parses configuration files and populates ServerConfig structures
class ConfigParser {
	private:


};